{
  "name": "react-bootstrap-input",
  "version": "0.0.1",
  "description": "A collection of ReactJS components which render bootstrap input elements.",
  "main": "lib/index.js",
  "module": "dist/react-bootstrap-input.es.js",
  "scripts": {
    "clean": "rimraf dist && rimraf docs",
    "build:babel": "babel src -d lib",
    "build": "npm run clean && npm run build:babel"
  },
  "author": "Can Bulut Bayburt",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "rimraf": "^2.6.2"
  },
  "dependencies": {
    "react": "^16.1.1"
  }
}
